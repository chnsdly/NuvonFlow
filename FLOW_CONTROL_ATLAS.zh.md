# Nuvon Flow Control Atlas 模块说明

## 模块目标

这是一套面向防腐热塑性阀门询价前期的决策支持模型。它帮助使用者从流体、任务、温度、压力、固体状态、执行方式和连接条件出发，形成：

- 材料、密封和阀型的初步评审方向；
- 需要优先暴露的风险；
- 仍需向客户、工厂或制造商确认的问题；
- 最终批准前所需的产品、测试、文件与法规证据；
- 可复制、下载或打印的 RFQ 初步评审摘要。

所有结果均为初步筛选，不构成化学兼容批准、压力等级承诺、认证声明或最终产品推荐。

## 与现有网站的边界

模块使用独立的内容页、页面模板、样式入口、脚本和数据文件。现有产品页、应用页、资源页和全站脚本不包含本模块的选择规则。

| 层 | 作用 |
|---|---|
| 双语内容页 | 提供英文和中文访问路径与搜索摘要 |
| Atlas 页面模板 | 负责页面结构，并只在该页面加载模块资源 |
| Atlas 样式 | 负责该工具的布局、响应式与打印样式 |
| Atlas 脚本 | 负责筛选、搜索、本地保存与导出 |
| JSON 数据模型 | 作为材料、密封、产品、流体、证据与界面文案的单一知识源 |

页面加载后，筛选和 RFQ 整理均在浏览器本地完成，不会由本模块上传询价数据。浏览器本地存储只用于保留该设备上的筛选状态和 RFQ 草稿。

## 维护方式

行业内容优先更新 JSON 数据模型，不要把新的材料判断或兼容性结论直接写入页面脚本。每次实质更新应：

1. 修改模型版本号；
2. 核对中英文含义是否一致；
3. 检查所有材料、密封和阀型引用是否仍然存在；
4. 用制造商当前资料复核新增技术信息；
5. 完成中英文构建后再发布。

涉及化学兼容性、压力—温度额定值、尺寸、认证或测试结果时，应保存出处与适用边界。不要从某一品牌或系列外推为整个材料家族的通用能力。

## 外部使用与导出

- 网站发布后，可直接通过英文或中文页面使用，无需进入聊天界面。
- “下载知识模型 JSON”可导出完整结构化模型，便于备份或以后接入 CRM、报价、培训、供应商审核等系统。
- RFQ 摘要可复制、下载为 TXT，或通过打印保存为 PDF。
- 如果以后需要完全独立的站点或内部工具，应继续读取同一 JSON 模型，避免维护两套互相漂移的行业规则。

## 剥离方式

移除双语 Atlas 内容页、Atlas 模板、Atlas 样式、Atlas 脚本和 Atlas JSON 数据即可完整剥离。当前实现没有修改现有产品与资源内容的数据结构，也没有把 Atlas 样式并入全站样式入口。
